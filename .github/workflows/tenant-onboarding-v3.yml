name: "🌱 Tenant Onboarding v3"

on:
  workflow_dispatch:
    inputs:
      tenants:
        description: 'Tenant names (comma-separated, e.g., "hospital-a,clinic-b") or "all" for all tenants'
        required: true
        default: 'all'
      dry_run:
        description: 'Dry run (validate config only, no deployment)'
        type: boolean
        default: false

concurrency:
  group: tenant-onboarding-v3-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write  # For creating GitHub releases (desktop)
  id-token: write  # For AWS OIDC

jobs:
  # ═══════════════════════════════════════════════════════════
  # JOB 1: DISCOVER TENANTS
  # ═══════════════════════════════════════════════════════════
  discover-tenants:
    name: Discover Tenants
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      tenants: ${{ steps.discover.outputs.tenants }}
      tenant_count: ${{ steps.count.outputs.tenant_count }}

    env:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Discover tenant environments
        id: discover
        run: bash .github/scripts/tenants/discover_tenants.sh "${{ github.repository }}" "${{ github.event.inputs.tenants }}"

      - name: Calculate tenant count
        id: count
        run: |
          TENANT_COUNT=$(echo '${{ steps.discover.outputs.tenants }}' | jq 'length')
          echo "tenant_count=$TENANT_COUNT" >> $GITHUB_OUTPUT
          echo "Found $TENANT_COUNT tenant(s) to process"

      - name: Summary
        run: |
          echo "## 🔍 Tenant Discovery" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Mode:** ${{ github.event.inputs.dry_run == 'true' && '🧪 Dry Run' || '🚀 Live Deployment' }}" >> $GITHUB_STEP_SUMMARY
          echo "**Tenants found:** ${{ steps.count.outputs.tenant_count }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Tenants to process:**" >> $GITHUB_STEP_SUMMARY
          echo '${{ steps.discover.outputs.tenants }}' | jq -r '.[] | "- " + .' >> $GITHUB_STEP_SUMMARY

      - name: Exit if no tenants to process
        if: fromJson(steps.count.outputs.tenant_count) == 0
        run: |
          echo "⚠️ No tenants to process. Exiting."
          exit 0

  # ═══════════════════════════════════════════════════════════
  # JOB 2: VALIDATE TENANT CONFIGURATIONS (BATCH)
  # ═══════════════════════════════════════════════════════════
  validate-configs:
    name: Validate (${{ matrix.tenant }})
    runs-on: ubuntu-latest
    needs: discover-tenants
    if: fromJson(needs.discover-tenants.outputs.tenant_count) > 0
    timeout-minutes: 5
    strategy:
      fail-fast: false
      matrix:
        tenant: ${{ fromJson(needs.discover-tenants.outputs.tenants) }}
    environment: ${{ matrix.tenant }}

    env:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
      INSTALLER_PUBLISHER: ${{ vars.INSTALLER_PUBLISHER }}
      INSTALLER_APP_NAME: ${{ vars.INSTALLER_APP_NAME }}
      INSTALLER_SUPPORT_URL: ${{ vars.INSTALLER_SUPPORT_URL }}
      AWS_IAM_ROLE_ARN: ${{ vars.AWS_IAM_ROLE_ARN }}
      AWS_REGION: ${{ vars.AWS_REGION }}
      BASE_URL: ${{ vars.BASE_URL }}
      TENANT_NAME: ${{ vars.TENANT_NAME }}
      COGNITO_POOL_ID: ${{ secrets.COGNITO_POOL_ID }}
      COGNITO_CLIENT_ID: ${{ secrets.COGNITO_CLIENT_ID }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Validate tenant configuration
        id: validate
        run: bash .github/scripts/tenants/validate_tenant_config.sh "${{ matrix.tenant }}" "onboarding"

      - name: Save validation status
        if: always()
        uses: ./.github/actions/save-tenant-status
        with:
          tenant: ${{ matrix.tenant }}
          phase: 'validation'
          status: ${{ steps.validate.outcome == 'success' && 'success' || 'failed' }}
          error: ${{ steps.validate.outcome != 'success' && 'Configuration validation failed' || '' }}
          config_snapshot: |
            {
              "AWS_REGION": "${{ env.AWS_REGION }}",
              "BASE_URL": "${{ env.BASE_URL }}",
              "TENANT_NAME": "${{ env.TENANT_NAME }}"
            }

  # ═══════════════════════════════════════════════════════════
  # JOB 3: DEPLOY TENANTS (CALLS REUSABLE WORKFLOW)
  # ═══════════════════════════════════════════════════════════
  deploy-tenants:
    name: Deploy (${{ matrix.tenant }})
    needs: [discover-tenants, validate-configs]
    if: github.event.inputs.dry_run != 'true' && fromJson(needs.discover-tenants.outputs.tenant_count) > 0
    strategy:
      fail-fast: false
      matrix:
        tenant: ${{ fromJson(needs.discover-tenants.outputs.tenants) }}
      max-parallel: 3
    uses: ./.github/workflows/_tenant-deploy-pipeline.yml
    with:
      tenant: ${{ matrix.tenant }}
      dry_run: ${{ github.event.inputs.dry_run == 'true' }}
    secrets: inherit

  # ═══════════════════════════════════════════════════════════
  # JOB 4: GENERATE FINAL REPORT
  # ═══════════════════════════════════════════════════════════
  final-report:
    name: Final Report
    runs-on: ubuntu-latest
    needs: [discover-tenants, validate-configs, deploy-tenants]
    if: always()
    timeout-minutes: 5

    steps:
      - name: Download all status artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: "status-*"
          path: all-status
          merge-multiple: true

      - name: Generate comprehensive report
        shell: bash
        run: |
          echo "# 🌱 Tenant Onboarding Report (v3)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Workflow Run:** [#${{ github.run_number }}](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})" >> $GITHUB_STEP_SUMMARY
          echo "**Mode:** ${{ github.event.inputs.dry_run == 'true' && '🧪 Dry Run' || '🚀 Live Deployment' }}" >> $GITHUB_STEP_SUMMARY
          echo "**Triggered by:** @${{ github.actor }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Calculate statistics
          TOTAL_TENANTS=0
          SUCCESS_COUNT=0
          FAILED_COUNT=0

          # Get unique tenant names
          TENANTS=$(ls all-status/*-validation.json 2>/dev/null | sed 's|all-status/||; s|-validation.json||' | sort -u || echo "")

          if [ -z "$TENANTS" ]; then
            echo "⚠️ No tenant status files found" >> $GITHUB_STEP_SUMMARY
            exit 0
          fi

          # For dry run, check validation only
          if [ "${{ github.event.inputs.dry_run }}" == "true" ]; then
            echo "## 📊 Validation Results" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "| Tenant | Validation | Config |" >> $GITHUB_STEP_SUMMARY
            echo "|--------|------------|--------|" >> $GITHUB_STEP_SUMMARY

            for TENANT in $TENANTS; do
              TOTAL_TENANTS=$((TOTAL_TENANTS + 1))

              VAL_FILE="all-status/${TENANT}-validation.json"
              if [ -f "$VAL_FILE" ]; then
                VAL_STATUS=$(jq -r '.status' "$VAL_FILE")

                if [ "$VAL_STATUS" == "success" ]; then
                  SUCCESS_COUNT=$((SUCCESS_COUNT + 1))
                  echo "| $TENANT | ✅ Valid | $(jq -r '.config.BASE_URL // "-"' "$VAL_FILE") |" >> $GITHUB_STEP_SUMMARY
                else
                  FAILED_COUNT=$((FAILED_COUNT + 1))
                  ERROR=$(jq -r '.error // "Unknown error"' "$VAL_FILE")
                  echo "| $TENANT | ❌ Invalid | ${ERROR:0:30}... |" >> $GITHUB_STEP_SUMMARY
                fi
              else
                FAILED_COUNT=$((FAILED_COUNT + 1))
                echo "| $TENANT | ⚠️ Missing | - |" >> $GITHUB_STEP_SUMMARY
              fi
            done
          else
            # Live deployment - check all phases
            echo "## 📊 Statistics" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY

            # Process each tenant
            declare -A TENANT_DATA
            for TENANT in $TENANTS; do
              TOTAL_TENANTS=$((TOTAL_TENANTS + 1))

              VAL_STATUS=$(jq -r '.status // "unknown"' "all-status/${TENANT}-validation.json" 2>/dev/null || echo "unknown")
              AMP_STATUS=$(jq -r '.status // "unknown"' "all-status/${TENANT}-amplify-init.json" 2>/dev/null || echo "unknown")
              POR_STATUS=$(jq -r '.status // "unknown"' "all-status/${TENANT}-portal-deploy.json" 2>/dev/null || echo "unknown")
              POS_STATUS=$(jq -r '.status // "unknown"' "all-status/${TENANT}-pos-deploy.json" 2>/dev/null || echo "unknown")

              # Determine overall status
              if [ "$VAL_STATUS" == "success" ] && [ "$AMP_STATUS" == "success" ] && [ "$POR_STATUS" == "success" ] && [ "$POS_STATUS" == "success" ]; then
                SUCCESS_COUNT=$((SUCCESS_COUNT + 1))
                TENANT_DATA[$TENANT]="success"
              else
                FAILED_COUNT=$((FAILED_COUNT + 1))
                TENANT_DATA[$TENANT]="failed"
              fi
            done

            # Display statistics
            echo "- **Total tenants:** $TOTAL_TENANTS" >> $GITHUB_STEP_SUMMARY
            echo "- **Successful:** $SUCCESS_COUNT ✅" >> $GITHUB_STEP_SUMMARY
            echo "- **Failed:** $FAILED_COUNT ❌" >> $GITHUB_STEP_SUMMARY

            if [ $TOTAL_TENANTS -gt 0 ]; then
              SUCCESS_RATE=$((SUCCESS_COUNT * 100 / TOTAL_TENANTS))
              echo "- **Success rate:** ${SUCCESS_RATE}%" >> $GITHUB_STEP_SUMMARY
            fi

            echo "" >> $GITHUB_STEP_SUMMARY

            # Detailed results table
            echo "## 📋 Detailed Results" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "| Tenant | Validation | Amplify | Portal | POS | Duration | Status |" >> $GITHUB_STEP_SUMMARY
            echo "|--------|------------|---------|--------|-----|----------|--------|" >> $GITHUB_STEP_SUMMARY

            for TENANT in $TENANTS; do
              # Read statuses
              VAL_STATUS=$(jq -r '.status // "unknown"' "all-status/${TENANT}-validation.json" 2>/dev/null || echo "unknown")
              AMP_STATUS=$(jq -r '.status // "unknown"' "all-status/${TENANT}-amplify-init.json" 2>/dev/null || echo "unknown")
              POR_STATUS=$(jq -r '.status // "unknown"' "all-status/${TENANT}-portal-deploy.json" 2>/dev/null || echo "unknown")
              POS_STATUS=$(jq -r '.status // "unknown"' "all-status/${TENANT}-pos-deploy.json" 2>/dev/null || echo "unknown")

              # Read durations
              AMP_DUR=$(jq -r '.duration // "0"' "all-status/${TENANT}-amplify-init.json" 2>/dev/null || echo "0")
              POR_DUR=$(jq -r '.duration // "0"' "all-status/${TENANT}-portal-deploy.json" 2>/dev/null || echo "0")
              POS_DUR=$(jq -r '.duration // "0"' "all-status/${TENANT}-pos-deploy.json" 2>/dev/null || echo "0")
              TOTAL_DUR=$((AMP_DUR + POR_DUR + POS_DUR))

              # Status icons
              VAL_ICON=$([ "$VAL_STATUS" == "success" ] && echo "✅" || echo "❌")
              AMP_ICON=$([ "$AMP_STATUS" == "success" ] && echo "✅" || echo "❌")
              POR_ICON=$([ "$POR_STATUS" == "success" ] && echo "✅" || ([ "$POR_STATUS" == "skipped" ] && echo "⏭️" || echo "❌"))
              POS_ICON=$([ "$POS_STATUS" == "success" ] && echo "✅" || ([ "$POS_STATUS" == "skipped" ] && echo "⏭️" || echo "❌"))

              # Overall status
              if [ "${TENANT_DATA[$TENANT]}" == "success" ]; then
                OVERALL="✅ Success"
              else
                OVERALL="❌ Failed"
              fi

              # Format duration
              if [ $TOTAL_DUR -ge 60 ]; then
                TOTAL_MIN=$((TOTAL_DUR / 60))
                TOTAL_SEC=$((TOTAL_DUR % 60))
                DURATION_STR="${TOTAL_MIN}m ${TOTAL_SEC}s"
              else
                DURATION_STR="${TOTAL_DUR}s"
              fi

              echo "| $TENANT | $VAL_ICON | $AMP_ICON | $POR_ICON | $POS_ICON | $DURATION_STR | $OVERALL |" >> $GITHUB_STEP_SUMMARY
            done

            echo "" >> $GITHUB_STEP_SUMMARY

            # Failure details section
            if [ $FAILED_COUNT -gt 0 ]; then
              echo "## ❌ Failure Details" >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY

              for TENANT in $TENANTS; do
                if [ "${TENANT_DATA[$TENANT]}" == "failed" ]; then
                  echo "<details><summary><b>$TENANT</b></summary>" >> $GITHUB_STEP_SUMMARY
                  echo "" >> $GITHUB_STEP_SUMMARY
                  echo "\`\`\`" >> $GITHUB_STEP_SUMMARY

                  for phase in validation amplify-init portal-deploy pos-deploy; do
                    FILE="all-status/${TENANT}-${phase}.json"
                    if [ -f "$FILE" ]; then
                      STATUS=$(jq -r '.status' "$FILE")
                      if [ "$STATUS" == "failed" ]; then
                        ERROR=$(jq -r '.error // "Unknown error"' "$FILE")
                        echo "[$phase] $ERROR" >> $GITHUB_STEP_SUMMARY
                      fi
                    fi
                  done

                  echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
                  echo "" >> $GITHUB_STEP_SUMMARY

                  # Resource links
                  echo "**Resources:**" >> $GITHUB_STEP_SUMMARY
                  for phase in amplify-init portal-deploy pos-deploy; do
                    FILE="all-status/${TENANT}-${phase}.json"
                    if [ -f "$FILE" ]; then
                      RESOURCES=$(jq -r '.resources // {}' "$FILE")
                      if [ "$RESOURCES" != "{}" ]; then
                        echo "$RESOURCES" | jq -r 'to_entries[] | "- \(.key): \(.value)"' >> $GITHUB_STEP_SUMMARY
                      fi
                    fi
                  done

                  echo "</details>" >> $GITHUB_STEP_SUMMARY
                  echo "" >> $GITHUB_STEP_SUMMARY
                fi
              done
            fi

            # Quick links
            echo "## 🔗 Quick Links" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "- [Workflow Run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})" >> $GITHUB_STEP_SUMMARY
            echo "- [AWS Amplify Console](https://console.aws.amazon.com/amplify/home)" >> $GITHUB_STEP_SUMMARY
            echo "- [GitHub Releases](https://github.com/${{ github.repository }}/releases)" >> $GITHUB_STEP_SUMMARY
          fi

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "*Generated at $(date -u +%Y-%m-%dT%H:%M:%SZ)*" >> $GITHUB_STEP_SUMMARY

          # Create JSON summary for next retry
          cat > summary.json <<EOF
          {
            "workflow_run": "${{ github.run_id }}",
            "mode": "${{ github.event.inputs.dry_run == 'true' && 'dry_run' || 'live' }}",
            "total_tenants": $TOTAL_TENANTS,
            "successful": $SUCCESS_COUNT,
            "failed": $FAILED_COUNT,
            "timestamp": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
            "tenants": [
          EOF

          FIRST=true
          for TENANT in $TENANTS; do
            if [ "$FIRST" == "true" ]; then
              FIRST=false
            else
              echo "," >> summary.json
            fi

            OVERALL_STATUS="${TENANT_DATA[$TENANT]}"
            cat >> summary.json <<EOF
            {
              "name": "$TENANT",
              "overall_status": "$OVERALL_STATUS"
            }
          EOF
          done

          cat >> summary.json <<EOF
            ]
          }
          EOF

      - name: Upload final report artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: final-report
          path: summary.json
          retention-days: 30

      - name: Determine workflow status
        shell: bash
        run: |
          # Get statistics from files
          TENANTS=$(ls all-status/*-validation.json 2>/dev/null | sed 's|all-status/||; s|-validation.json||' | sort -u || echo "")

          if [ -z "$TENANTS" ]; then
            echo "⚠️ No tenants processed"
            exit 1
          fi

          FAILED_COUNT=0

          # For dry run, check validation only
          if [ "${{ github.event.inputs.dry_run }}" == "true" ]; then
            for TENANT in $TENANTS; do
              VAL_STATUS=$(jq -r '.status // "unknown"' "all-status/${TENANT}-validation.json" 2>/dev/null || echo "unknown")
              if [ "$VAL_STATUS" != "success" ]; then
                FAILED_COUNT=$((FAILED_COUNT + 1))
              fi
            done

            if [ $FAILED_COUNT -gt 0 ]; then
              echo "❌ Workflow failed: $FAILED_COUNT tenant(s) have invalid configuration"
              exit 1
            else
              echo "✅ Workflow successful: All tenant configurations are valid"
              exit 0
            fi
          fi

          # For live deployment, check if any tenant failed
          for TENANT in $TENANTS; do
            VAL_STATUS=$(jq -r '.status // "unknown"' "all-status/${TENANT}-validation.json" 2>/dev/null || echo "unknown")
            AMP_STATUS=$(jq -r '.status // "unknown"' "all-status/${TENANT}-amplify-init.json" 2>/dev/null || echo "unknown")
            POR_STATUS=$(jq -r '.status // "unknown"' "all-status/${TENANT}-portal-deploy.json" 2>/dev/null || echo "unknown")
            POS_STATUS=$(jq -r '.status // "unknown"' "all-status/${TENANT}-pos-deploy.json" 2>/dev/null || echo "unknown")

            if [ "$VAL_STATUS" != "success" ] || [ "$AMP_STATUS" != "success" ] || [ "$POR_STATUS" != "success" ] || [ "$POS_STATUS" != "success" ]; then
              FAILED_COUNT=$((FAILED_COUNT + 1))
            fi
          done

          if [ $FAILED_COUNT -gt 0 ]; then
            echo "⚠️ Workflow completed with failures: $FAILED_COUNT tenant(s) failed"
            exit 1
          else
            echo "✅ Workflow successful: All tenants deployed successfully"
            exit 0
          fi
